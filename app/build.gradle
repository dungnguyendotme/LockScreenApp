apply plugin: 'com.android.application'
android {
    compileSdkVersion 8
    buildToolsVersion "27.0.1"
    defaultConfig {
        applicationId "com.lockscreen.vietlinkads"
        minSdkVersion 8
        targetSdkVersion 8
        versionCode 4
        versionName '1.0'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    productFlavors {
    }
}

dependencies {
}